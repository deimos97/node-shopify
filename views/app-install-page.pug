extends layout

block content
	h1(style="color: green;") App Installed!
	h2= title
	h3(style="color: green;") D:
	each paramValue, paramIndex in shopifyParams
		pre= paramIndex + " : " + paramValue
	
	